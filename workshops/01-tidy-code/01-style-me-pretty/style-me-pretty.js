// // YOUR CODE BELOW
function styleMePretty(){
  let dataValue = 'Unformatted code';
  let returnValue = `${dataValue} is difficult to parse`;
  returnValue += ' for humans if not for machines';
  return returnValue;
}





// function styleMePretty() {
//       let truth = '';
// function innerBeauty(count) {
//   let result = '';
//   while(count > 0) {
//     count--;
//       if(count===2) {
//       result += 'Unformatted code';
//       }
//   } result += ' is difficult to parse';
//   return result;} truth += innerBeauty(5); truth += ' for humans if not for machines';
// return truth;
// }
